DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'export',
        'USER': 'export',
        'PASSWORD': 'export',
        'HOST': 'localhost',
        'PORT': '',  # Live empty for default.
    }
}

DEBUG = True
